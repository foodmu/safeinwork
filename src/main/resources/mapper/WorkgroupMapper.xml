<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jiuheng.mapper.WorkgroupMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.jiuheng.entity.Workgroup">
        <id column="workgroupId" property="workgroupId" />
        <result column="workgroupName" property="workgroupName" />
        <result column="parentId" property="parentId" />
        <result column="leaderId" property="leaderId" />
        <result column="member" property="member" />
        <result column="pId" property="pId" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        workgroupId, workgroupName, parentId, leaderId, member, pId
    </sql>

</mapper>
